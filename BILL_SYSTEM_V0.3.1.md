# Bill System v0.3.1 面试技术展陈规划

状态：方案 A 已确认、已实现并完成本机验收；方案 B/XA 未实施
规划日期：2026-09-13

员工编号：MYPROJECT-GPT5-001

实现基线：Bill System v0.2，Git Commit 24a1332
目标岗位：字节跳动中国交易与广告——生活服务商业平台全栈开发方向

> 本文取代 v0.3 并冻结为方案 A 的范围基准。BILL_SYSTEM_V0.3.md 仅作为历史规划保留。Kafka 方案 A 的真实完成证据记录在 `docs/REVIEW_V0.3.1.md`；Seata XA 仍为 DESIGN_ONLY，不计入已完成功能。

## 1. 版本定位

Bill System v0.3.1 不是待上线的电商系统，而是一个面向面试官、可交互的分布式交易技术实验场。

目标是在有限时间内让面试官看到候选人能够：

1. 把业务问题翻译成技术问题；
2. 为不同场景选择同步、异步、强一致或最终一致方案；
3. 主动制造并定位故障；
4. 用数据库、消息、事务日志、Trace 和自动化测试交叉证明结论；
5. 解释机制的收益、代价和适用边界；
6. 区分概念了解、真实实现、测试通过和生产可用。

核心展示句：

> 我没有尝试用一种技术解决整条交易链路，而是把通信方式和一致性策略拆开：普通流量走同步 HTTP，高峰请求使用 Kafka 削峰；异步受理与 Outbox 使用本地事务，跨服务通过幂等、补偿、恢复任务和对账实现最终一致。XA 只作为短事务强一致的设计对比，不冒充本版本实现。

## 2. v0.2 基线与 v0.3.1 增量

### 2.1 v0.2 已完成，必须保留

- Order、Inventory、Payment 三个 Spring Boot 服务；
- 三个独立 MySQL Schema；
- 客户端、服务端运维台、数据库操作台三个页面；
- 请求幂等、库存并发控制和防超卖；
- 超时、限流、熔断、隔离和补偿；
- 支付回调丢失、Job Runner、死信和主动对账；
- Trace、操作审计和 WebSocket 刷新通知；
- v0.1 九类可靠性回归、v0.2 六道联动 Gate；
- k6 本地性能基线和一键启动、重置、验收脚本。

### 2.2 完整候选增量（方案 A + 方案 B）

- Kafka 异步请求、Outbox、幂等消费、重试和 DLQ；
- 同步与异步模式的公平压测和 Lag 排空证据；
- Seata XA 正常提交、整体回滚与锁等待实验；
- 支付最终一致性与 XA 强一致的同屏对比；
- 真实业务事件驱动的 WebSocket 技术证据；
- 一致性规则检测、证据 Diff 和受控恢复；
- 90 秒、5 分钟、10 分钟三档演示脚本。

### 2.3 本次冻结的方案 A 范围

本次实际完成 Kafka、Outbox、幂等消费、重试/DLQ、Lag、同步/异步压测、业务事件 WebSocket 和 v0.2 支付最终一致性复用。下文 XA_LAB、Gate 6 至 8 和 Seata 组件属于方案 B 的 `DESIGN_ONLY` 内容，不在当前页面、运行栈或完成清单中。

## 3. 正确的技术分类：两个维度

### 3.1 维度一：通信方式

| 方案 | 解决的问题 | 收益 | 代价 |
|---|---|---|---|
| 同步 HTTP | 普通流量下立即得到结果 | 链路简单、响应直观 | 下游慢会拖慢请求，故障容易沿调用链传播 |
| Kafka 异步 | 高峰接入、削峰、解耦 | 请求可排队，下游按稳定速率处理 | 用户只能先得到已受理，系统必须管理积压、重复和最终结果 |

### 3.2 维度二：一致性策略

| 方案 | 解决的问题 | 收益 | 代价 |
|---|---|---|---|
| Seata XA | 短时间、内部可控的多资源共同提交或回滚 | 强一致直观，失败时整体回滚 | Prepare 后可能持锁等待，吞吐与可用性下降 |
| 最终一致性 | 支付、消息、长流程和外部系统 | 不持有长事务，可恢复、可扩展 | 允许短暂不一致，需要幂等、重试、补偿、对账和人工兜底 |

### 3.3 四个精选实验

四个实验不是四套系统，而是同一业务在不同问题下的受控对比：

1. SYNC_RELIABILITY：同步 HTTP、本地事务、超时、熔断和补偿；
2. KAFKA_PEAK：Kafka、Outbox、幂等消费者、重试和 DLQ；
3. XA_LAB：订单与库存短事务的 Seata XA 提交和回滚；
4. PAYMENT_EVENTUAL：支付回调、恢复任务和对账的最终一致性。

## 4. 面试展示闭环标准

每个技术点必须同时回答五个问题，缺一项就不进入主展示。

| 层次 | 面试官看到什么 | 合格证据 |
|---|---|---|
| 问题 | 为什么需要它 | 明确的业务故障或容量问题 |
| 操作 | 如何触发 | 一个按钮或一条固定脚本可复现 |
| 过程 | 内部发生了什么 | 实时状态、Trace、XID、Offset、Lag 或 Job 变化 |
| 事实 | 最终结果是否真实 | MySQL、Kafka、Seata 或审计数据交叉验证 |
| 结论 | 为什么选、代价是什么 | 一句话结论与明确适用边界 |

页面上的每项声明标注证据等级：

- IMPLEMENTED：真实代码和进程存在；
- VERIFIED：正常、异常和恢复测试通过；
- BENCHMARKED：有环境、负载和原始结果的性能基线；
- SIMULATED：业务对象为本地模拟，但机制真实运行；
- DESIGN_ONLY：只用于生产演进讨论，不冒充已实现。

## 5. 单入口与系统边界

~~~text
Browser :3100
  ├─ /client       → 业务入口与单请求技术证据
  ├─ /operations   → Kafka、XA、Trace、Job、性能与故障
  ├─ /database     → 一致性证据、检测、注入、修复与审计
  ├─ /api/orders/**
  ├─ /inventory-api/**
  ├─ /payment-api/**
  └─ /ws/events

Internal
  ├─ Order Service :8785
  ├─ Inventory Service :8786
  ├─ Payment Service :8787
  ├─ MySQL 8：bill_order / bill_inventory / bill_payment
  ├─ Redis 7
  ├─ Kafka：单 Broker、KRaft、本地演示
  └─ Seata Server：仅 XA_LAB 使用
~~~

约束：

- 浏览器只访问 127.0.0.1:3100；
- 服务只能写自己的 Schema；
- 正常业务不通过跨 Schema SQL 协作；
- Kafka、WebSocket 和前端状态均不是业务最终事实；
- 单 MySQL 容器、不同 Schema 只能证明服务和事务边界，不能冒充跨机房数据库集群；
- Kafka 单 Broker 只能证明消息机制，不能证明 Broker 高可用。

## 6. 三个页面

### 6.1 客户端 /client

客户端默认启用面试展示模式，同时展示业务结果和技术证据。

业务层：

- 商品、价格、可用库存和已预占库存；
- 普通同步下单、高峰异步受理；
- 正常支付、支付成功但回调丢失；
- 取消、释放库存和退款；
- 已受理、排队中、处理中、成功、拒绝和失败。

订单技术抽屉：

- 实验类型、请求 ID、订单 ID、幂等键和 Trace ID；
- Topic、Partition、Offset、Message Key 和 Consumer Group；
- 接入、排队、消费、业务处理和端到端耗时；
- XID、分支数量和全局事务结果；
- WebSocket Event ID、Sequence 和 Event Type；
- 最终事实查询接口与最近刷新时间。

客户端不直接调用 Inventory 或 Payment 写接口，也不直接修改数据库。

### 6.2 服务端运维台 /operations

这是主展示页，使用标签页避免所有模块同时争夺注意力。

#### Overview

- Order、Inventory、Payment、Kafka、Seata、MySQL、Redis 健康状态；
- 当前同步请求、异步请求、Kafka Lag、恢复任务和 WebSocket 连接；
- 当前实验、Git Commit、数据集版本和证据等级。

#### Transaction

- 按请求 ID、订单 ID、Trace ID 或 XID 搜索；
- 对齐订单、库存、支付、Outbox、消息、Job 和审计；
- 同屏显示业务状态时间线与技术事件时间线；
- 保留 v0.2 显式订单状态机。

#### Kafka Lab

- Topic、Partition、Consumer Group、当前 Offset 和 Log End Offset；
- 生产速率、消费速率、Lag 和最老消息等待时间；
- 受理、排队、处理中、成功、业务拒绝、技术失败数量；
- 重试、DLQ、重复投递和幂等拦截；
- 高峰结束后的积压排空曲线；
- 可展开一条消息查看完整链路。

#### XA Lab

- XID、全局状态、订单分支和库存分支；
- Prepare、Commit 或 Rollback 的真实阶段；
- 正常提交、库存分支失败和第二阶段延迟三个固定实验；
- 锁等待、锁持有时间、全局事务耗时和竞争请求延迟；
- 回滚前后数据库事实对比。

#### Reliability

- v0.2 的库存失败、库存延迟、支付失败和回调丢失；
- Job Runner、死信和人工重跑；
- 熔断、限流和隔离状态；
- Kafka 消费暂停、技术失败和重复投递；
- WebSocket 主动断开与恢复；
- 一键清除故障，但不伪造或抹掉已导出的证据。

#### Performance

- 同步完整链路与异步受理、最终完成的分口径结果；
- 输入速率、处理速率、错误率、业务拒绝率、P50/P95/P99；
- Kafka Lag、排空时间和下游稳定性；
- 环境、数据集、脚本哈希、Git Commit 和原始结果路径。

### 6.3 数据一致性控制台 /database

页面改名为数据一致性控制台，不提供任意 SQL。

四个区域：

1. 真实数据：按服务和 Schema 展示订单、请求、Outbox、预占、支付、Job、消息索引和审计；
2. 一致性检测：显示规则、严重程度、涉及记录、可能原因和建议动作；
3. 事故注入：库存归零、消息重复、消费者失败、XA 分支失败等固定实验；
4. 受控恢复：生成计划、用户确认、Admin API 执行、前后 Diff、审计和复检。

优先检测规则：

- 支付 SUCCEEDED，但订单仍为 PENDING_PAYMENT；
- 订单取消，但库存预占仍有效；
- 同一幂等键产生多个业务结果；
- 同一订单和 SKU 存在多个有效预占；
- Outbox 长时间未发布；
- 消息已消费但业务结果缺失；
- XA 已结束但业务事实不符合预期；
- Job 或消息超过最大重试次数。

## 7. 实验一：SYNC_RELIABILITY

沿用 v0.2 成熟链路：

~~~text
Client
→ Order Service 登记订单
→ Inventory Service 预占库存
→ Payment Service 创建模拟支付单
→ Order 进入 PENDING_PAYMENT
~~~

主展示：

- 同一幂等键重放仍返回原订单；
- 并发争抢最后库存不超卖；
- Inventory 超时后请求按边界失败，不无限挂起；
- Payment 连续失败打开熔断器；
- 取消或失败后释放库存；
- Trace 和数据库能够解释每一步。

一句话结论：

> 同步链路适合普通流量，但必须限制故障传播，并让补偿动作保持幂等。

## 8. 实验二：KAFKA_PEAK

### 8.1 主链路

~~~text
Client
→ Order Service 本地事务写 order_requests 与 outbox_events
→ 返回 HTTP 202 + requestId
→ Outbox Relay 发布 order-create-command
→ Consumer 幂等处理
→ 调用既有库存和支付创建能力
→ 持久化最终业务结果
→ 发布状态事件
→ WebSocket 通知页面重新查询最终事实
~~~

规则：

- 202 只代表可靠受理，不代表订单创建成功；
- 请求登记和 Outbox 必须同一本地事务提交；
- Relay 允许重复发布；
- Consumer 先完成业务结果持久化，再提交 Offset；
- Consumer 可能重复收到消息，数据库唯一约束和处理记录保证业务只执行一次；
- 库存不足属于业务拒绝，不进入技术重试；
- 网络和临时数据库异常可有限重试，超限进入 DLQ；
- Kafka 只缓冲和削平流量，不增加下游真实处理能力。

### 8.2 最小 Topic 集

| Topic | 用途 | Key | Consumer Group |
|---|---|---|---|
| order-create-command | 异步下单命令 | requestId | order-create-v1 |
| order-create-retry | 可重试技术失败 | requestId | order-create-retry-v1 |
| order-create-dlq | 超限消息 | requestId | 人工处理 |
| order-status-event | 最终状态通知 | orderId | state-observer-v1 |

支付事件不作为首轮 Kafka 实现的必选项，避免与现有 Job Runner 和对账逻辑重复建设。

### 8.3 必演场景

1. 高峰输入高于消费能力，Lag 上升；
2. 停止输入后 Consumer 继续处理，Lag 归零；
3. 同一 requestId 重复投递，收到多次但业务执行一次；
4. 技术失败进入重试或 DLQ，人工重放仍保持幂等。

一句话结论：

> Kafka 把瞬时压力转化为可观察、可控制的排队时间，代价是业务完成从同步结果变成最终结果。

## 9. 实验三：XA_LAB（DESIGN_ONLY，方案 A 未实现）

### 9.1 范围

XA 只覆盖订单和库存的短事务实验，不覆盖支付。

~~~text
Order Service 开启全局事务
→ Order 分支写入订单
→ Inventory 分支扣减并创建预占
→ 全部分支 Prepare 成功：Commit
→ 任一分支失败：Rollback
→ XA 结束后才进入支付流程
~~~

### 9.2 真实证据来源

XA 业务记录回滚后可能完全消失，因此不能依赖订单表保存失败证据。

必须从事务外保存观察事实：

- Seata Server 的全局事务和分支事务状态；
- 独立 transaction_experiments 表，记录 runId、traceId、xid、场景和最终结果；
- XA 完成后追加的入库的实验审计；
- Trace 记录真实调用结果，但不能把前端动画冒充 Prepare/Commit。

transaction_experiments 不参与 XA，不包含真实业务敏感数据，只保存实验元数据。

### 9.3 三个固定场景

1. 正常提交：订单、库存和预占一起生效；
2. 库存分支失败：订单、库存和预占均不残留；
3. 第二阶段延迟：竞争请求出现锁等待和延迟上升，恢复后得到可解释结果。

### 9.4 真实性边界

- XID和分支状态必须来自真实 Seata 或事务日志；
- 当前三个 Schema 位于同一个 MySQL 容器，不能宣称跨数据库集群；
- 无法可靠读取的 XA 阶段不显示，不生成假时间线；
- Seata Server 恢复能力只在真实验证后宣称；
- XA 模式的价值是对比和教学，不代表生产默认方案。

一句话结论：

> XA 用持锁和协调换取短事务强一致，因此适合内部可控的短链路，不适合等待用户和外部机构的支付过程。

## 10. 实验四：PAYMENT_EVENTUAL

沿用并增强 v0.2：

~~~text
Payment 写入 SUCCEEDED
→ 回调被故障开关丢弃
→ Order 暂停在 PENDING_PAYMENT
→ 一致性检测器发现差异
→ Job Runner 或主动对账查询支付事实
→ 幂等推进 Order 为 PAID
→ WebSocket 通知三页刷新
~~~

主展示：

- 短暂不一致是真实数据库事实；
- 恢复任务持久化，不依赖浏览器；
- 重试、对账和人工恢复都走同一状态机；
- 重复回调不会重复推进；
- 无法自动判断的结果进入 MANUAL_REVIEW；
- 每次修复都有 Trace 和审计。

一句话结论：

> 外部支付不能参加数据库长事务，系统要接受短暂不一致，并用可重试、可对账、可人工兜底的流程最终收敛。

## 11. WebSocket 的展示边界

v0.3.1 将 v0.2 的定时刷新提示升级为真实业务事件通知，但 WebSocket 仍不提交交易。

事件最小信封：

~~~text
eventId
sequence
eventType
aggregateType
aggregateId
occurredAt
traceId
payloadVersion
~~~

必做：

- 状态变化时推送事件，不再只靠固定每秒广播；
- 页面显示连接、最近心跳、最后事件序号和最近事件；
- 断线自动重连；
- 重连后重新查询 HTTP API，确保最终结果不依赖消息是否补发。

后置项：

- 按 orderId 或 requestId 精细订阅；
- 事件持久化与完整断点续传；
- 多实例连接路由。

一句话结论：

> WebSocket只负责让变化及时可见，数据库和公开查询接口才负责让业务事实可靠。

## 12. 同步与异步压测

### 12.1 公平原则

- 相同机器、Docker资源、数据分布和代码版本；
- 相同输入到达率和测试阶段；
- 每组独立重置数据；
- 固定 k6 脚本并记录哈希；
- 结果记录时间、Git Commit和原始文件；
- 不把异步 202 接入延迟与同步完整业务延迟直接比较。

### 12.2 必须分开的指标

同步模式：

- 完整订单链路 P50、P95、P99；
- 吞吐、HTTP错误率、业务拒绝率；
- 超时、熔断、限流和库存不变量。

Kafka模式：

- HTTP受理 P50、P95、P99；
- 受理到最终结果 P50、P95、P99；
- 生产速率、消费速率、Lag峰值和排空时间；
- 队列等待时间、业务处理时间；
- 重试、DLQ、重复投递和幂等拦截；
- 下游处理速率是否保持稳定。

### 12.3 推荐实验负载

~~~text
预热：10 VU，10秒
爬升：10 → 50 VU，20秒
高峰：50 VU，20秒
突发：100 VU，10秒
排空：停止生产后观察最多60秒
~~~

允许受控限制 Consumer 处理速率以稳定制造 Lag，但页面和报告必须明确这是实验参数。

正确结论：

> 异步模式可能更快接受请求并保护下游，但不会凭空提高最终业务吞吐；当输入持续超过处理能力时，Lag 仍会无限增长，最终必须告警、限流或拒绝。

## 13. 自动验收 Gate

### Gate 1：基线不退化

- v0.1 九场景全部通过；
- v0.2 六道三端联动 Gate 全部通过；
- start、reset、verify 仍可一键执行。

### Gate 2：Outbox 原子性

- 请求与 Outbox 同时提交或同时失败；
- Kafka不可用时已受理请求不静默丢失；
- Relay恢复后可以继续发布。

### Gate 3：重复消息与幂等

- 同一 requestId 重复发送；
- 页面显示接收次数大于一；
- 订单、支付单和有效预占均只产生一份；
- 数据库唯一约束提供证据。

### Gate 4：重试与 DLQ

- 技术错误按上限退避重试；
- 业务拒绝不重试；
- 超限消息进入 DLQ；
- 人工重放后成功或给出明确失败，且不重复执行业务。

### Gate 5：削峰与排空

- 输入速率高于消费速率时 Lag 上升；
- 下游处理速率维持在设定范围；
- 停止生产后 Lag 在超时内归零；
- 最终成功数、拒绝数和库存变化守恒。

### Gate 6：XA正常提交

- XID和两个分支真实存在；
- 订单、库存和预占共同提交；
- 页面事实与数据库一致。

### Gate 7：XA失败回滚

- 库存分支失败触发全局回滚；
- 不残留业务订单、库存扣减或有效预占；
- 事务外实验记录和Seata状态能够解释失败。

### Gate 8：XA锁代价

- 第二阶段延迟可重复触发；
- 竞争请求出现可观测锁等待或延迟增长；
- 解除故障后事务收敛；
- 报告不把实验结果外推为生产性能。

### Gate 9：支付最终一致性

- 支付成功和订单待支付的差异真实存在；
- Job或对账完成幂等修复；
- 重复回调不重复推进；
- Trace、审计和两库事实一致。

### Gate 10：WebSocket断线

- 断线期间后端继续处理；
- 重连后页面通过查询接口获得最终事实；
- 页面刷新不改变任何业务状态。

## 14. 面试演示脚本

### 14.1 90秒电梯版

1. 20秒：展示一个入口、三个页面、三个服务和真实数据；
2. 35秒：启动Kafka高峰，展示Lag上升与下游处理速率稳定；
3. 20秒：展开一条消息，展示Outbox、Offset、幂等和最终订单；
4. 15秒：展示自动Gate，并说明XA与支付最终一致性用于追问。

### 14.2 5分钟标准版

1. 30秒：问题地图与两维架构；
2. 60秒：同步幂等和最后库存并发；
3. 90秒：Kafka高峰、Lag、排空和重复消费；
4. 60秒：支付丢回调、差异检测与对账恢复；
5. 45秒：XA库存分支失败和整体回滚；
6. 15秒：测试证据和生产边界。

### 14.3 10分钟深挖版

在5分钟版本基础上，由面试官选择一个方向展开：

- 消息：Outbox、Offset提交窗口、重复消费、重试和DLQ；
- 事务：XID、Prepare、Rollback、锁等待和支付不参加XA；
- 稳定性：超时、熔断、隔离、限流和补偿；
- 测试：不变量、故障注入、Gate、压测口径和Bad Case。

原则：不强行把所有能力讲完，主流程短，证据可按追问展开。

## 15. 不作为现场阻塞项

以下能力保留在生产演进问答，不进入面试前核心验收：

- API Gateway、登录、RBAC、审批和双人复核；
- Kafka多Broker、副本、跨机房和灾备；
- MySQL主从、备份、迁移和容灾；
- Kubernetes、服务网格、配置中心和密钥平台；
- 多实例WebSocket连接路由；
- 完整OpenTelemetry、Prometheus和告警平台；
- 所有页面和所有边缘场景的Web自动化；
- 真实支付渠道和真实用户数据。

## 16. 实施包与时间评估

时间从v0.2已通过基线开始估算，前提是Docker镜像和依赖可以正常取得。

### 方案 A：Kafka优先面试核心版，推荐

预计：10至16小时。

包含：

- Outbox、Kafka异步受理、幂等消费者；
- 重复投递、有限重试和DLQ；
- Kafka Lab与单条消息证据；
- 同步/异步压测和Lag排空；
- 保留并包装v0.2支付最终一致性；
- 业务事件驱动WebSocket；
- Gate 1至5、Gate 9至10；
- 90秒和5分钟演示脚本。

不包含真实Seata XA，只在架构说明中作为DESIGN_ONLY，不在页面伪造。

优点：增量价值最高、故障面可控、最贴合高并发交易与工程测试。
风险：面试前仍需连续开发、构建和演练。

### 方案 B：完整v0.3.1

预计：24至40小时。

在方案A基础上增加：

- 真实Seata Server和XA数据源接入；
- 正常提交、分支回滚和协调延迟；
- 事务外实验记录和XA Lab；
- Gate 6至8；
- 完整10分钟深挖脚本。

优点：技术对比最完整，能深入讨论分布式事务。
风险：依赖接入、事务传播、阶段证据和本机启动稳定性都可能占用较长调试时间；面试前完成并熟练讲解的风险高。

### 方案 C：展示增强版

预计：4至7小时。

包含：

- 不增加Kafka或Seata；
- 重构运维台信息层级；
- 增加问题、实验、证据、结论卡片；
- 增加90秒和5分钟脚本；
- 完善现有自动验收结果展示。

优点：风险最低，可集中时间熟悉现有能力。
缺点：技术增量有限，不满足“更多、更深”的完整目标。

## 17. 推荐决策

考虑面试日期为2026-09-14，推荐选择方案A。

原因：

1. v0.2已经证明同步可靠性和支付最终一致性；
2. Kafka、Outbox、重复消费、Lag和压测是当前最明显的新能力；
3. Kafka链路能够同时展示后端、前端、数据库、中间件、测试和性能；
4. 真实XA值得做，但如果无法完成真实阶段证据，仓促实现反而容易被追问击穿；
5. 方案A完成后仍可把Seata XA作为面试后的v0.3.2扩展。

如果时间不受面试日期限制并且目标是长期作品集，选择方案B。

## 18. 已确认的决策

确认实施前，请逐项检查：

- [x] 选择方案 A：Kafka 优先面试核心版；
- [x] “通信方式”和“一致性策略”分成两个维度；
- [x] 运维台作为主展示，数据库页提供真实证据；
- [x] Kafka 使用单 Broker 本地实验，不宣传高可用；
- [x] 单 MySQL 容器多 Schema 不宣传跨集群；
- [x] 202 受理延迟与最终完成延迟分开统计；
- [x] Seata XA 不在页面伪造，保留为 DESIGN_ONLY；
- [x] 生产建设项移出面试前核心验收。

## 19. 完成后的交付物

- BILL_SYSTEM_V0.3.1.md：冻结后的范围和验收基准；
- docs/ARCHITECTURE_V0.3.1.md：两维架构、Topic、事务和数据所有权；
- docs/INTERVIEW_DEMO_V0.3.1.md：三档演示脚本和追问；
- docs/PERFORMANCE_BASELINE_V0.3.1.md：对比结果与原始证据索引；
- docs/REVIEW_V0.3.1.md：不可覆盖的逐项证据Review；
- verify-v0.3.1.cmd与新增自动Gate；
- 可追溯的k6原始结果；
- Kafka、Trace、数据库和审计的真实运行证据；XA 只有设计说明，不算运行证据。

## 20. 证据状态

方案 A 已按以下证据规则实现并通过；具体逐项记录见 `docs/REVIEW_V0.3.1.md`。方案 B 的 XA 内容不因本文存在而视为完成。

任何能力只有同时满足以下条件后才能标记完成：

1. 真实代码和配置存在；
2. 真实进程或基础设施参与；
3. 页面数据来自真实接口或运行指标；
4. 数据库、Kafka、Seata、Trace或审计可交叉验证；
5. 正常、异常和恢复自动验收通过；
6. REVIEW_V0.3.1.md明确记录结论、证据强度和适用边界。

## 21. 技术参考

- Apache Kafka消息投递语义：https://kafka.apache.org/08/design/design/
- Apache Kafka Consumer Offset与Lag API：https://kafka.apache.org/42/javadoc/org/apache/kafka/clients/consumer/KafkaConsumer.html
- Apache Seata XA模式：https://seata.apache.org/docs/user/mode/xa
