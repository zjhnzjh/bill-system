# Bill System v0.2 目标规划

状态：v0.2 已实现并通过本机验收，本文是唯一范围与验收基准  
规划日期：2026-09-13  
员工编号：`MYPROJECT-GPT5-001`  
目标岗位：字节跳动中国交易与广告——生活服务商业平台全栈开发方向

## 1. 升级目标

v0.2 不再只是把九个可靠性场景集中放在一个演示页面，而是把生活服务订单系统拆成三个可同时打开、实时联动的工作界面：

1. **客户端**：模拟用户浏览套餐、提交订单、支付、取消和查看结果；
2. **服务端运维台**：观察订单服务、库存服务、支付服务、Trace、恢复任务和容错状态；
3. **数据库操作台**：查看真实数据表，通过受控操作改变库存或支付事实，并保存审计记录。

核心演示闭环：

```text
数据库操作台改变真实数据
→ 服务端读取并处理新状态
→ WebSocket 通知三个界面刷新
→ 客户端出现成功或失败结果
→ 运维台展示调用链、状态变化、失败位置和恢复结果
```

## 2. 三个界面

### 2.1 客户端 `/client`

- 展示生活服务商品、价格和剩余库存；
- 自动生成幂等键，支持重复提交同一请求；
- 创建订单并预占库存；
- 正常支付、模拟支付成功但回调丢失；
- 取消未支付订单、退款已支付订单；
- 展示用户能理解的失败提示，例如商品不存在、库存不足、服务超时；
- 展示当前订单状态时间线，但不暴露数据库管理能力。

### 2.2 服务端运维台 `/operations`

- 展示三个服务的实时健康状态和固定端口；
- 展示订单、库存预占和支付记录的关联关系；
- 展示订单状态机的每次推进；
- 按 Trace ID 展示调用步骤、结果、耗时和错误详情；
- 展示 Job Runner 的状态、执行次数、下次执行时间、最后错误和死信状态；
- 支持对死信任务进行受控人工重跑；
- 展示支付熔断器、库存/支付故障开关和恢复状态；
- 展示 WebSocket 连接状态与最后同步时间。

### 2.3 数据库操作台 `/database`

- 分表展示 `orders`、`inventory_items`、`inventory_reservations`、`payments`、`recovery_jobs` 和审计记录；
- 展示数据属于哪个服务和哪个 MySQL Schema；
- 支持把演示商品可用库存设置为 `0`、`1`、`10` 或自定义非负数；
- 支持在没有有效预占时删除演示商品，并一键恢复；
- 支持将模拟支付事实受控推进为 `SUCCEEDED` 或 `REFUNDED`；
- 所有后台修改真实写入 MySQL，并记录操作者、动作、变更前后和时间；
- 不提供任意 SQL 输入框，不允许修改系统表或执行无边界删除。

## 3. 必须演示的三端联动

### 场景 A：后台删商品，客户端下单失败

1. 数据库操作台删除 `LIFE-DEMO-001`；
2. 库存表立即显示该记录不存在，并新增 `DELETE_ITEM` 审计；
3. 客户端无需手工刷新即可看到商品不可用；
4. 用户尝试下单，订单进入 `FAILED`；
5. 运维台 Trace 明确标记失败发生在 `inventory.reserve`，详情为 `Unknown SKU`；
6. 后台恢复商品后，客户端可再次创建新订单。

### 场景 B：后台库存归零，客户端库存不足

1. 数据库操作台把可用库存改为 `0`；
2. 客户端实时显示售罄；
3. 下单返回明确的库存不足结果，不创建支付单；
4. 运维台能证明库存没有变成负数、支付服务没有被调用；
5. 审计表记录库存由原值变为 `0`。

### 场景 C：支付事实与订单状态暂时不一致

1. 创建待支付订单；
2. 在数据库操作台把对应支付事实推进为 `SUCCEEDED`；
3. 客户端订单仍为 `PENDING_PAYMENT`，直观展示跨服务数据不会自动形成同一事务；
4. 运维台执行主动对账；
5. 订单恢复为 `PAID`，Trace 出现 `payment.reconcile` 和 `payment.callback` 证据。

### 场景 D：支付回调丢失后自动恢复

1. 客户端执行“支付但丢回调”；
2. 支付表变为 `SUCCEEDED`，订单暂时保持 `PENDING_PAYMENT`；
3. 恢复任务真实写入 `recovery_jobs`；
4. Job Runner 自动查询支付事实并推进订单；
5. 三个界面同步显示任务成功和订单变为 `PAID`。

## 4. WebSocket 实时同步

- 浏览器连接 `/ws/events`；
- 后端在数据可能变化时推送状态刷新事件；
- 三个页面收到事件后重新读取各服务公开的查询接口；
- 页面显示“已连接 / 重连中 / 已断开”和最后同步时间；
- 断线自动重连，WebSocket 只负责通知，不把它当作数据唯一来源；
- 页面重新加载后仍以数据库内容为准，证明数据不是只存在浏览器内存中。

## 5. 数据与权限边界

- Order Service 只能写 `bill_order`；
- Inventory Service 只能写 `bill_inventory`；
- Payment Service 只能写 `bill_payment`；
- 服务端业务链路通过 HTTP API 协作，不跨库直接改其他服务数据；
- 数据库操作台调用各服务提供的受控 Admin API；
- 演示环境使用固定操作者 `interview-admin`，审计记录不可由页面修改；
- 删除商品前必须确认不存在有效库存预占，防止退款或取消时无法释放库存；
- v0.2 不实现真正账号体系和生产级 RBAC，页面必须标明这一边界。

## 6. 压测验收页

压测页的目的不是只展示 P50/P95 数字，而是回答五个问题：

| 问题 | v0.2 展示内容 |
|---|---|
| 压了什么 | 完整创建订单链路：订单落库、库存预占、支付单创建、Trace 记录 |
| 怎么压 | k6 阶梯加压，最长 40 秒、最高 20 VU |
| 在什么环境压 | Ryzen 7 5800H、约 16GB 内存、Docker 单机单实例 |
| 结果如何 | 1804 请求、45.04 req/s、0% HTTP 失败、P50 139.00ms、P95 286.99ms、P99 344.64ms |
| 能说明什么 | 仅证明本地基线稳定，不代表生产容量或线上 SLA |

页面同时解释：

- `P50`：一半请求比这个值更快；
- `P95`：95% 请求在这个时间内完成，观察尾部慢请求；
- `P99`：最慢的 1% 请求边界；
- 吞吐量：系统单位时间完成多少请求；
- 错误率：高压下有多少请求失败；
- 通过标准：自动检查通过、错误率符合阈值、P95 符合阈值、测试数据可清理、结果文件可追溯。

v0.2 仍使用已有真实 k6 基线，不伪造实时生产监控数据。启动压测继续通过 `load-test.cmd` 完成。

## 7. 技术实现范围

- React + TypeScript 保持单一前端工程，提供三个独立路由；
- Java 21 + Spring Boot 保持 Order、Inventory、Payment 三服务；
- MySQL 保持三个独立 Schema；
- Redis 继续承担限流状态；
- Resilience4j 继续承担支付熔断和并发隔离；
- Spring WebSocket 提供实时变更通知；
- 新增库存后台操作审计和支付后台操作审计；
- `start.cmd`、`verify.cmd`、`load-test.cmd`、`reset-demo.cmd` 保持可用；
- 重置脚本必须同时清理 v0.2 审计数据，并恢复固定商品库存。

## 8. 验收标准

### 8.1 页面验收

- [x] `/client`、`/operations`、`/database` 均可独立打开；
- [x] 三个页面导航清晰，面试时不需要修改 URL；
- [x] WebSocket 状态可见，数据库变化能在 2 秒内反映到其他页面；
- [x] 客户端不出现后台删除、改库存等管理按钮；
- [x] 运维台能把同一订单、支付、库存预占、Job 和 Trace 关联起来；
- [x] 数据库操作台明确展示 Schema、表名和真实记录。

### 8.2 数据验收

- [x] 库存修改、删除和恢复均真实写入 `bill_inventory`；
- [x] 支付状态受控推进真实写入 `bill_payment`；
- [x] 每次后台修改都有审计记录；
- [x] 删除商品后客户端下单失败，恢复后新订单成功；
- [x] 支付事实变为成功后，主动或自动对账能推进订单；
- [x] 刷新浏览器或重启前端后数据仍然存在。

### 8.3 回归验收

- [x] `start.cmd` 一键启动并通过全部健康检查；
- [x] v0.1 九项可靠性自动验收继续通过；
- [x] v0.2 三端联动新增验收脚本通过；
- [x] 前端生产构建通过；
- [x] k6 基线结果与解释可在运维台查看；
- [x] `reset-demo.cmd` 能恢复干净的演示初始状态。

## 9. 明确不做

- 不接真实支付机构；
- 不使用真实用户或商家数据；
- 不内置任意 SQL 执行器；
- 不把本地 k6 数据包装成线上容量结论；
- 不在面试前引入 Kafka、Kubernetes、完整登录系统或复杂 RBAC；
- 不为了视觉效果伪造数据库记录、Trace 或 Job 状态。

## 10. 实施顺序

1. 建立库存和支付审计表及受控 Admin API；
2. 补齐库存、预占、支付、订单、Job、Trace 查询接口；
3. 建立 WebSocket 通知与断线重连；
4. 拆分三个前端页面并完成真实数据关联；
5. 补充压测解释和既有基线展示；
6. 增加 v0.2 联动验收，重新运行九项回归；
7. 保存不可覆盖的 `docs/REVIEW_V0.2.0.md` 并更新面试演示手册。

## 11. 证据状态

代码、运行界面、真实数据库记录、自动化验收和浏览器检查已共同提供完成证据。逐项核验见 `docs/REVIEW_V0.2.0.md`。
